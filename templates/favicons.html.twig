{% block favicons %}
    {% import 'macros.html.twig' as html %}

    {% for favicon in favicons %}
        {{ html.link(favicon.href, favicon) }}
    {% endfor %}

    {{ html.link('img/favicons/manifest.json', {'rel': 'manifest'}) }}
{% endblock %}
